# prebuilt/kernel is the gzip-compressed ARM64 Image committed to fit the
# 32MB boot partition. Ignore any locally-extracted raw (uncompressed)
# Image backup so it is never committed by accident.
prebuilt/kernel-*.raw
