#!/bin/bash
#
# vendorsetup.sh for ZYB ZPD1203 (k69v1_64_k419)
#
# Lunch combos are registered via COMMON_LUNCH_CHOICES in
# AndroidProducts.mk (Android 12+ no longer uses add_lunch_combo).
# This file is kept for device-tree convention but intentionally
# does NOT call the obsolete add_lunch_combo.
